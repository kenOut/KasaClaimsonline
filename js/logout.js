function logout() {
  // Perform logout operations if needed (e.g., clear session storage, cookies, etc.)
  alert("You have been logged out.");
  window.location.href = "../auth/login.html"; // Redirect to login page
}
